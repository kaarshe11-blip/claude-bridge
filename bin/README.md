# bin

Place the local Claude Code executable here when using the same layout as the working Codex install:

```text
bin/claude.exe
```

The executable is intentionally not committed. Set `CLAUDE_CODE_COMMAND` to the full local path of the Claude Code CLI.
